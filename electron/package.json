{
  "name": "mcplexer-desktop",
  "version": "0.1.0",
  "description": "MCPlexer Desktop — MCP Gateway with Web UI",
  "main": "dist/main.js",
  "scripts": {
    "dev": "tsc && electron .",
    "build": "tsc",
    "package": "tsc && electron-builder",
    "package:mac": "tsc && electron-builder --mac",
    "package:win": "tsc && electron-builder --win",
    "package:linux": "tsc && electron-builder --linux"
  },
  "devDependencies": {
    "electron": "~33.3.1",
    "electron-builder": "~25.1.8",
    "typescript": "~5.7.3"
  },
  "build": {
    "extends": "electron-builder.yml"
  }
}
